# Contact — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implémenter le sous-projet "Contact" du backend NestJS (table `contact_message` + 6 endpoints + 2 templates + throttling) selon le spec `2026-04-26-contact-design.md`. ~18 nouveaux tests, total projet ~192. Premier consommateur réel du `MailerService` livré au sous-projet précédent (7a).

**Architecture:** `ContactModule` standard (controller + service + 2 DTOs + 2 templates HTML). Service consomme `DRIZZLE` (DB), `AppConfigService` (env), `MailerService` (envoi mail, @Global). Helper transversal `parsePagination` dans `src/common/`. Throttler global via `@nestjs/throttler` + `APP_GUARD`, override fin (5/60s) sur `POST /contact/messages`. Stratégie mails fire-and-forget après DB insert : visiteur reçoit 201 rapidement, mails partent en arrière-plan.

**Tech Stack:** NestJS 11, Drizzle ORM, `@nestjs/throttler` (rate limiting), `class-validator` + `class-transformer` (DTOs + query coercion), `@nestjs/swagger`, Jest + `createMockDb()` partagé + mock MailerService.

**Référence spec :** `docs/superpowers/specs/2026-04-26-contact-design.md`

---

## File Structure

### Fichiers à créer

| Chemin | Rôle |
|---|---|
| `src/database/schema/contact-messages.ts` | Table Drizzle `contact_message` + types |
| `src/common/pagination.ts` | `parsePagination` + types `PaginationParams`/`PaginatedResult<T>` |
| `src/common/pagination.spec.ts` | 5 tests purs |
| `src/contact/contact.module.ts` | Module standard, imports AuthModule |
| `src/contact/contact.controller.ts` | 6 endpoints + Swagger |
| `src/contact/contact.service.ts` | 5 méthodes publiques + `sendNotificationMails` privé |
| `src/contact/contact.service.spec.ts` | 13 tests via `createMockDb` + mocks |
| `src/contact/dto/create-contact-message.dto.ts` | name/email/subject/message validés |
| `src/contact/dto/list-contact-messages.dto.ts` | page/limit query params |
| `src/contact/mail-templates/contact-notification.html` | Port 1:1 Hono (~69 lignes) |
| `src/contact/mail-templates/contact-confirmation.html` | Port 1:1 Hono (~52 lignes) |

### Fichiers à modifier

| Chemin | Modification |
|---|---|
| `package.json` | +`@nestjs/throttler` |
| `pnpm-lock.yaml` | Auto par pnpm |
| `src/config/env.schema.ts` | +3 env vars `CONTACT_*` |
| `src/config/env.validation.spec.ts` | +tests pour les nouvelles vars |
| `src/config/app-config.service.ts` | +3 getters `contact*` |
| `.env.example` | +3 env vars documentées |
| `.env` | +3 env vars dev |
| `src/database/schema/index.ts` | +export `contact-messages` |
| `src/app.module.ts` | +`ThrottlerModule.forRoot(...)` + `ContactModule` + `APP_GUARD` |
| `nest-cli.json` | +`assets` + `watchAssets` |
| `README.md` | +section `## Contact` + liste sous-projets renumérotée |

---

## Task 1: Install deps + extension env validation

**Files:**
- Modify: `package.json`, `pnpm-lock.yaml`
- Modify: `src/config/env.schema.ts`
- Modify: `src/config/env.validation.spec.ts`
- Modify: `src/config/app-config.service.ts`
- Modify: `.env.example`
- Modify: `.env`

- [ ] **Step 1: Installer la dep prod**

```bash
pnpm add @nestjs/throttler
```

Expected: pnpm résout sans erreur, ajoute `@nestjs/throttler` dans `dependencies`.

- [ ] **Step 2: Étendre `src/config/env.schema.ts`**

Ouvrir le fichier. Trouver la fin de l'objet `envSchema` (qui se termine par les SMTP vars) :

```typescript
  // Mailer (SMTP)
  SMTP_HOST: z.string().min(1),
  SMTP_PORT: z.coerce.number().int().min(1).max(65535).default(587),
  SMTP_SECURE: z
    .string()
    .default('false')
    .transform((v) => v === 'true' || v === '1'),
  SMTP_USER: z.string().min(1),
  SMTP_PASS: z.string().min(1),
  SMTP_FROM: z.string().email(),
});
```

Insérer les 3 nouvelles vars CONTACT juste avant la fermeture `})`, après `SMTP_FROM` :

```typescript
  // Mailer (SMTP)
  SMTP_HOST: z.string().min(1),
  SMTP_PORT: z.coerce.number().int().min(1).max(65535).default(587),
  SMTP_SECURE: z
    .string()
    .default('false')
    .transform((v) => v === 'true' || v === '1'),
  SMTP_USER: z.string().min(1),
  SMTP_PASS: z.string().min(1),
  SMTP_FROM: z.string().email(),

  // Contact
  CONTACT_EMAIL: z.string().email(),
  CONTACT_PHONE: z.string().min(1),
  CONTACT_LOCATION: z.string().min(1),
});
```

- [ ] **Step 3: Étendre `src/config/env.validation.spec.ts`**

Ouvrir le fichier. La constante `baseValid` doit être étendue avec les 3 vars CONTACT pour que les tests existants passent (validation Zod va rejeter sinon).

État actuel (extrait) :

```typescript
  const baseValid = {
    DATABASE_URL: 'postgres://u:p@localhost:55432/db',
    JWT_SECRET: '0123456789abcdef0123456789abcdef',
    S3_ENDPOINT: 'http://localhost:9000',
    S3_REGION: 'us-east-1',
    S3_ACCESS_KEY: 'access-key',
    S3_SECRET_KEY: 'secret-key-12345',
    SMTP_HOST: 'localhost',
    SMTP_USER: 'mailpit',
    SMTP_PASS: 'mailpit',
    SMTP_FROM: 'noreply@test.local',
  };
```

Le remplacer par (ajout 3 fields) :

```typescript
  const baseValid = {
    DATABASE_URL: 'postgres://u:p@localhost:55432/db',
    JWT_SECRET: '0123456789abcdef0123456789abcdef',
    S3_ENDPOINT: 'http://localhost:9000',
    S3_REGION: 'us-east-1',
    S3_ACCESS_KEY: 'access-key',
    S3_SECRET_KEY: 'secret-key-12345',
    SMTP_HOST: 'localhost',
    SMTP_USER: 'mailpit',
    SMTP_PASS: 'mailpit',
    SMTP_FROM: 'noreply@test.local',
    CONTACT_EMAIL: 'admin@test.local',
    CONTACT_PHONE: '+33 6 00 00 00 00',
    CONTACT_LOCATION: 'Lyon, France',
  };
```

Puis ajouter ces 3 nouveaux tests à la fin du `describe('validateEnv', ...)` (juste avant la fermeture `});` du fichier) :

```typescript
  it('rejette CONTACT_EMAIL si pas un email valide', () => {
    expect(() =>
      validateEnv({ ...baseValid, CONTACT_EMAIL: 'not-an-email' }),
    ).toThrow(/CONTACT_EMAIL/);
  });

  it('rejette CONTACT_PHONE vide', () => {
    expect(() => validateEnv({ ...baseValid, CONTACT_PHONE: '' })).toThrow(
      /CONTACT_PHONE/,
    );
  });

  it('rejette CONTACT_LOCATION vide', () => {
    expect(() =>
      validateEnv({ ...baseValid, CONTACT_LOCATION: '' }),
    ).toThrow(/CONTACT_LOCATION/);
  });
```

- [ ] **Step 4: Étendre `src/config/app-config.service.ts`**

Ouvrir le fichier. À la fin de la classe, juste après le getter `smtpFrom()`, ajouter les 3 nouveaux getters :

```typescript
  get contactEmail() {
    return this.config.get('CONTACT_EMAIL', { infer: true });
  }
  get contactPhone() {
    return this.config.get('CONTACT_PHONE', { infer: true });
  }
  get contactLocation() {
    return this.config.get('CONTACT_LOCATION', { infer: true });
  }
```

Placer AVANT la fermeture `}` de la classe.

- [ ] **Step 5: Étendre `.env.example`**

Ouvrir `.env.example`. Ajouter à la fin :

```bash

# Contact
CONTACT_EMAIL=admin@nedellec-julien.fr
CONTACT_PHONE=+33 6 00 00 00 00
CONTACT_LOCATION=Lyon, France
```

- [ ] **Step 6: Étendre `.env`**

Le `.env` du dev local doit aussi contenir ces vars pour que l'app puisse booter. Vérifier d'abord si `.env` est tracké par git :

```bash
git ls-files .env
```

Si la sortie est vide, `.env` est gitignoré (cas habituel) — éditer mais NE PAS commit.

Ajouter à la fin de `.env` :

```bash

# Contact
CONTACT_EMAIL=admin@nedellec-julien.fr
CONTACT_PHONE=+33 6 00 00 00 00
CONTACT_LOCATION=Lyon, France
```

- [ ] **Step 7: Vérifier les tests env**

```bash
pnpm test src/config/env.validation.spec.ts
```

Expected: tous verts (existants + 3 nouveaux).

- [ ] **Step 8: Vérifier la suite globale**

```bash
pnpm test
```

Expected: tous verts (~177 = 174 + 3 new).

- [ ] **Step 9: Vérifier le build**

```bash
pnpm build
```

Expected: clean.

- [ ] **Step 10: Lint**

```bash
pnpm lint
```

Expected: clean.

- [ ] **Step 11: Commit**

```bash
git add package.json pnpm-lock.yaml src/config/env.schema.ts src/config/env.validation.spec.ts src/config/app-config.service.ts .env.example
git commit -m "feat(deps,config): @nestjs/throttler + extension env CONTACT_*"
```

> Si `.env` est tracké (étape 6 a montré qu'il est listé), inclure aussi `.env` dans `git add`. Sinon, ne pas l'inclure.

---

## Task 2: Pagination helper TDD

**Files:**
- Create: `src/common/pagination.ts`
- Create: `src/common/pagination.spec.ts`

- [ ] **Step 1: Écrire les tests d'abord (TDD)**

Créer `src/common/pagination.spec.ts` avec ce contenu exact :

```typescript
import { parsePagination } from './pagination';

describe('parsePagination', () => {
  it('utilise les defaults si rien fourni', () => {
    expect(parsePagination({})).toEqual({ page: 1, limit: 10, offset: 0 });
  });

  it('cape page < 1 à 1', () => {
    expect(parsePagination({ page: 0 })).toEqual({
      page: 1,
      limit: 10,
      offset: 0,
    });
  });

  it('cape limit < 1 à 1', () => {
    expect(parsePagination({ limit: 0 })).toEqual({
      page: 1,
      limit: 1,
      offset: 0,
    });
  });

  it('cape limit > 100 à 100', () => {
    expect(parsePagination({ limit: 200 })).toEqual({
      page: 1,
      limit: 100,
      offset: 0,
    });
  });

  it('calcule offset correctement', () => {
    expect(parsePagination({ page: 3, limit: 20 })).toEqual({
      page: 3,
      limit: 20,
      offset: 40,
    });
  });
});
```

- [ ] **Step 2: Lancer les tests pour vérifier qu'ils échouent**

```bash
pnpm test src/common/pagination.spec.ts
```

Expected: erreur `Cannot find module './pagination'`. Normal — l'implémentation n'existe pas.

- [ ] **Step 3: Créer l'implémentation**

Créer `src/common/pagination.ts` :

```typescript
export interface PaginationParams {
  page: number;
  limit: number;
  offset: number;
}

export interface PaginatedResult<T> {
  data: T[];
  total: number;
  page: number;
  limit: number;
}

const DEFAULT_PAGE = 1;
const DEFAULT_LIMIT = 10;
const MAX_LIMIT = 100;

export function parsePagination(input: {
  page?: number;
  limit?: number;
}): PaginationParams {
  const page = Math.max(1, input.page ?? DEFAULT_PAGE);
  const limit = Math.min(
    MAX_LIMIT,
    Math.max(1, input.limit ?? DEFAULT_LIMIT),
  );
  const offset = (page - 1) * limit;
  return { page, limit, offset };
}
```

- [ ] **Step 4: Lancer les tests pour vérifier qu'ils passent**

```bash
pnpm test src/common/pagination.spec.ts
```

Expected: 5 tests verts.

- [ ] **Step 5: Lancer la suite complète**

```bash
pnpm test
```

Expected: ~182 tests verts (~177 + 5 new).

- [ ] **Step 6: Lint**

```bash
pnpm lint
```

Expected: clean.

- [ ] **Step 7: Commit**

```bash
git add src/common/pagination.ts src/common/pagination.spec.ts
git commit -m "feat(common): parsePagination helper + types + 5 tests"
```

---

## Task 3: Schéma DB + barrel + migration

**Files:**
- Create: `src/database/schema/contact-messages.ts`
- Modify: `src/database/schema/index.ts`
- Generated: `drizzle/0003_*.sql`

- [ ] **Step 1: Créer le fichier schéma**

Créer `src/database/schema/contact-messages.ts` :

```typescript
import {
  pgTable,
  uuid,
  text,
  boolean,
  timestamp,
  index,
} from 'drizzle-orm/pg-core';
import { sql } from 'drizzle-orm';

export const contactMessages = pgTable(
  'contact_message',
  {
    id: uuid('id')
      .primaryKey()
      .default(sql`gen_random_uuid()`),
    name: text('name').notNull(),
    email: text('email').notNull(),
    subject: text('subject').notNull(),
    message: text('message').notNull(),
    read: boolean('read').notNull().default(false),
    createdAt: timestamp('created_at', { withTimezone: true })
      .notNull()
      .defaultNow(),
  },
  (t) => ({
    readIdx: index('contact_message_read_idx').on(t.read),
    createdAtIdx: index('contact_message_created_at_idx').on(t.createdAt),
    readCreatedIdx: index('contact_message_read_created_idx').on(
      t.read,
      t.createdAt,
    ),
  }),
);

export type ContactMessage = typeof contactMessages.$inferSelect;
export type NewContactMessage = typeof contactMessages.$inferInsert;
```

- [ ] **Step 2: Étendre le barrel `src/database/schema/index.ts`**

Ouvrir `src/database/schema/index.ts`. Ajouter `contact-messages` aux 3 endroits (import, re-export, schema spread).

Trouver la dernière ligne d'imports (devrait être `import * as projects from './projects';`). Ajouter immédiatement après :

```typescript
import * as contactMessages from './contact-messages';
```

Trouver la dernière ligne `export *` (devrait être `export * from './projects';`). Ajouter immédiatement après :

```typescript
export * from './contact-messages';
```

Trouver le spread dans `schema` (devrait se terminer par `...projects,`). Ajouter immédiatement après :

```typescript
  ...contactMessages,
```

- [ ] **Step 3: Vérifier que TypeScript compile**

```bash
pnpm build
```

Expected: clean.

- [ ] **Step 4: Démarrer Postgres si pas déjà**

```bash
pnpm db:up && pnpm db:wait
```

Expected: container `portfolio-nest-db` healthy.

- [ ] **Step 5: Générer la migration Drizzle**

```bash
pnpm db:generate
```

Expected:
- Drizzle Kit détecte la nouvelle table `contact_message` + 3 indexes.
- Crée `drizzle/0003_<random_name>.sql`.
- Le contenu doit inclure `CREATE TABLE "contact_message" (...)` avec `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`, et 3 `CREATE INDEX` sur `read`, `created_at`, et composite.

- [ ] **Step 6: Inspecter la migration**

Ouvrir le fichier `drizzle/0003_*.sql`. Vérifier :
- ✅ Pas de DROP sur les tables existantes
- ✅ Pas d'INSERT manuel
- ✅ Les 3 indexes sont présents avec les noms `contact_message_read_idx`, `contact_message_created_at_idx`, `contact_message_read_created_idx`

- [ ] **Step 7: Appliquer la migration**

```bash
pnpm db:migrate
```

Expected: clean.

- [ ] **Step 8: Vérifier la table en DB**

```bash
podman exec portfolio-nest-db psql -U portfolio -d portfolio_nest -c "\d contact_message"
```

Expected: 7 colonnes (id, name, email, subject, message, read, created_at) + 3 indexes (read, created_at, read+created_at) + PK.

- [ ] **Step 9: Commit**

```bash
git add src/database/schema/contact-messages.ts src/database/schema/index.ts drizzle/
git commit -m "feat(contact): schéma Drizzle + migration 0003"
```

---

## Task 4: Templates HTML + nest-cli.json assets

**Files:**
- Create: `src/contact/mail-templates/contact-notification.html`
- Create: `src/contact/mail-templates/contact-confirmation.html`
- Modify: `nest-cli.json`

- [ ] **Step 1: Copier `contact-notification.html` depuis le backend Hono**

Le template existe dans le backend Hono à `/home/jned/WebstormProjects/J-Ned/angular-portfolio-app/server/mail-templates/contact-notification.html`. Le copier tel quel :

```bash
mkdir -p src/contact/mail-templates
cp /home/jned/WebstormProjects/J-Ned/angular-portfolio-app/server/mail-templates/contact-notification.html src/contact/mail-templates/contact-notification.html
```

Vérifier la copie :

```bash
wc -l src/contact/mail-templates/contact-notification.html
```

Expected: ~69 lignes.

- [ ] **Step 2: Copier `contact-confirmation.html`**

```bash
cp /home/jned/WebstormProjects/J-Ned/angular-portfolio-app/server/mail-templates/contact-confirmation.html src/contact/mail-templates/contact-confirmation.html
```

Vérifier :

```bash
wc -l src/contact/mail-templates/contact-confirmation.html
```

Expected: ~52 lignes.

- [ ] **Step 3: Vérifier que les variables sont compatibles**

Vérifier que les templates utilisent bien `{{name}}`, `{{email}}`, `{{subject}}`, `{{message}}` (compatible avec notre `renderTemplate`) :

```bash
grep -oE '\{\{[a-z]+\}\}' src/contact/mail-templates/contact-notification.html | sort -u
grep -oE '\{\{[a-z]+\}\}' src/contact/mail-templates/contact-confirmation.html | sort -u
```

Expected:
- `contact-notification.html` : `{{email}}`, `{{message}}`, `{{name}}`, `{{subject}}`
- `contact-confirmation.html` : `{{name}}`, `{{subject}}` (uniquement ces 2)

- [ ] **Step 4: Modifier `nest-cli.json`**

Remplacer le contenu de `nest-cli.json` par :

```json
{
  "$schema": "https://json.schemastore.org/nest-cli",
  "collection": "@nestjs/schematics",
  "sourceRoot": "src",
  "compilerOptions": {
    "deleteOutDir": true,
    "assets": [
      { "include": "**/mail-templates/**/*.html" }
    ],
    "watchAssets": true
  }
}
```

- [ ] **Step 5: Vérifier que le build copie les templates**

```bash
pnpm build
ls dist/contact/mail-templates/
```

Expected: les 2 fichiers `.html` présents dans `dist/contact/mail-templates/`.

Si les fichiers ne sont pas copiés, double-checker la syntaxe `assets` (doit être un array d'objets avec `include`).

- [ ] **Step 6: Commit**

```bash
git add src/contact/mail-templates/ nest-cli.json
git commit -m "feat(contact): templates HTML (port Hono) + nest-cli assets pattern"
```

---

## Task 5: DTOs Contact

**Files:**
- Create: `src/contact/dto/create-contact-message.dto.ts`
- Create: `src/contact/dto/list-contact-messages.dto.ts`

- [ ] **Step 1: Créer `CreateContactMessageDto`**

Créer `src/contact/dto/create-contact-message.dto.ts` :

```typescript
import { ApiProperty } from '@nestjs/swagger';
import {
  IsEmail,
  IsNotEmpty,
  IsString,
  MaxLength,
} from 'class-validator';

export class CreateContactMessageDto {
  @ApiProperty({ maxLength: 200 })
  @IsString()
  @IsNotEmpty()
  @MaxLength(200)
  name!: string;

  @ApiProperty({ format: 'email', maxLength: 320 })
  @IsEmail()
  @MaxLength(320)
  email!: string;

  @ApiProperty({ maxLength: 200 })
  @IsString()
  @IsNotEmpty()
  @MaxLength(200)
  subject!: string;

  @ApiProperty({ maxLength: 5000 })
  @IsString()
  @IsNotEmpty()
  @MaxLength(5000)
  message!: string;
}
```

- [ ] **Step 2: Créer `ListContactMessagesDto`**

Créer `src/contact/dto/list-contact-messages.dto.ts` :

```typescript
import { ApiPropertyOptional } from '@nestjs/swagger';
import { Type } from 'class-transformer';
import { IsInt, IsOptional, Max, Min } from 'class-validator';

export class ListContactMessagesDto {
  @ApiPropertyOptional({ default: 1, minimum: 1 })
  @IsOptional()
  @Type(() => Number)
  @IsInt()
  @Min(1)
  page?: number;

  @ApiPropertyOptional({ default: 10, minimum: 1, maximum: 100 })
  @IsOptional()
  @Type(() => Number)
  @IsInt()
  @Min(1)
  @Max(100)
  limit?: number;
}
```

- [ ] **Step 3: Vérifier le build**

```bash
pnpm build
```

Expected: clean.

- [ ] **Step 4: Commit**

```bash
git add src/contact/dto/
git commit -m "feat(contact): DTOs CreateContactMessage + ListContactMessages"
```

---

## Task 6: Service + tests TDD

**Files:**
- Create: `src/contact/contact.service.ts`
- Create: `src/contact/contact.service.spec.ts`

- [ ] **Step 1: Écrire les tests d'abord (TDD)**

Créer `src/contact/contact.service.spec.ts` :

```typescript
/* eslint-disable @typescript-eslint/unbound-method */
import { Test, TestingModule } from '@nestjs/testing';
import { NotFoundException } from '@nestjs/common';
import { ContactService } from './contact.service';
import { DRIZZLE } from '../database/drizzle.constants';
import { createMockDb } from '../database/test-utils';
import { AppConfigService } from '../config/app-config.service';
import { MailerService } from '../mailer/mailer.service';
import type { ContactMessage } from '../database/schema/contact-messages';

describe('ContactService', () => {
  let service: ContactService;
  let db: ReturnType<typeof createMockDb>;
  let cfg: { contactEmail: string };
  let mailer: jest.Mocked<MailerService>;

  const mkMessage = (overrides: Partial<ContactMessage> = {}): ContactMessage => ({
    id: '11111111-1111-1111-1111-111111111111',
    name: 'Visitor',
    email: 'visitor@example.com',
    subject: 'Hello',
    message: 'Hi there!',
    read: false,
    createdAt: new Date('2026-04-26T00:00:00Z'),
    ...overrides,
  });

  beforeEach(async () => {
    db = createMockDb();
    cfg = { contactEmail: 'admin@test.local' };
    mailer = {
      sendMail: jest.fn().mockResolvedValue(undefined),
    } as unknown as jest.Mocked<MailerService>;

    const module: TestingModule = await Test.createTestingModule({
      providers: [
        ContactService,
        { provide: DRIZZLE, useValue: db },
        { provide: AppConfigService, useValue: cfg as unknown as AppConfigService },
        { provide: MailerService, useValue: mailer },
      ],
    }).compile();
    service = module.get(ContactService);
  });

  describe('create', () => {
    it('insère un message et retourne la row', async () => {
      const created = mkMessage();
      db.returning.mockResolvedValueOnce([created]);
      const result = await service.create({
        name: 'Visitor',
        email: 'visitor@example.com',
        subject: 'Hello',
        message: 'Hi there!',
      });
      expect(result).toEqual(created);
    });

    it('retourne immédiatement, sans attendre les mails', async () => {
      const created = mkMessage();
      db.returning.mockResolvedValueOnce([created]);
      // Mailer met 100ms à répondre
      mailer.sendMail.mockImplementation(
        () => new Promise((resolve) => setTimeout(resolve, 100)),
      );
      const start = Date.now();
      await service.create({
        name: 'V',
        email: 'v@x.com',
        subject: 's',
        message: 'm',
      });
      const elapsed = Date.now() - start;
      // Le `create` doit retourner bien avant les 100ms × 2 mails
      expect(elapsed).toBeLessThan(50);
    });

    it('appelle MailerService.sendMail 2 fois (admin + visitor)', async () => {
      const created = mkMessage();
      db.returning.mockResolvedValueOnce([created]);
      await service.create({
        name: 'V',
        email: 'v@x.com',
        subject: 's',
        message: 'm',
      });
      // Attendre que les promises fire-and-forget se résolvent
      await new Promise((resolve) => setImmediate(resolve));
      expect(mailer.sendMail).toHaveBeenCalledTimes(2);
    });

    it('envoie le mail admin à cfg.contactEmail avec sujet "Nouveau message de contact: <subject>"', async () => {
      const created = mkMessage({ subject: 'Demande de devis' });
      db.returning.mockResolvedValueOnce([created]);
      await service.create({
        name: 'V',
        email: 'v@x.com',
        subject: 'Demande de devis',
        message: 'm',
      });
      await new Promise((resolve) => setImmediate(resolve));
      const adminCall = mailer.sendMail.mock.calls.find(
        (call) => call[0].to === 'admin@test.local',
      );
      expect(adminCall).toBeDefined();
      expect(adminCall![0].subject).toBe(
        'Nouveau message de contact: Demande de devis',
      );
      expect(adminCall![0].html).toContain('Demande de devis');
    });

    it('envoie le mail confirmation à dto.email avec sujet "Confirmation de votre message"', async () => {
      const created = mkMessage({ email: 'visitor@example.com' });
      db.returning.mockResolvedValueOnce([created]);
      await service.create({
        name: 'V',
        email: 'visitor@example.com',
        subject: 's',
        message: 'm',
      });
      await new Promise((resolve) => setImmediate(resolve));
      const visitorCall = mailer.sendMail.mock.calls.find(
        (call) => call[0].to === 'visitor@example.com',
      );
      expect(visitorCall).toBeDefined();
      expect(visitorCall![0].subject).toBe('Confirmation de votre message');
    });

    it('resolve quand même si MailerService.sendMail reject (fire-and-forget)', async () => {
      const created = mkMessage();
      db.returning.mockResolvedValueOnce([created]);
      mailer.sendMail.mockRejectedValue(new Error('SMTP down'));
      // Le create ne doit pas throw même si les mails échouent
      await expect(
        service.create({
          name: 'V',
          email: 'v@x.com',
          subject: 's',
          message: 'm',
        }),
      ).resolves.toEqual(created);
    });
  });

  describe('findAll', () => {
    it('retourne le résultat paginé { data, total, page, limit }', async () => {
      const rows = [mkMessage({ id: 'a' }), mkMessage({ id: 'b' })];
      // findAll fait 2 queries en parallèle :
      // 1) select.from.orderBy.limit.offset → rows
      // 2) select(count).from → totalRow
      // Avec createMockDb, on doit mocker chaque query séparément.
      // db.offset retourne le builder (par défaut), il faut résoudre via l'await final.
      db.offset.mockResolvedValueOnce(rows);
      db.from.mockResolvedValueOnce([{ count: 42 }]);
      const result = await service.findAll({
        page: 2,
        limit: 5,
        offset: 5,
      });
      expect(result).toEqual({
        data: rows,
        total: 42,
        page: 2,
        limit: 5,
      });
    });
  });

  describe('unreadCount', () => {
    it('retourne { count } filtré sur read: false', async () => {
      db.where.mockResolvedValueOnce([{ count: 7 }]);
      const result = await service.unreadCount();
      expect(result).toEqual({ count: 7 });
    });

    it('retourne count: 0 si aucun row', async () => {
      db.where.mockResolvedValueOnce([]);
      const result = await service.unreadCount();
      expect(result).toEqual({ count: 0 });
    });
  });

  describe('markRead', () => {
    it('met read: true et retourne la row', async () => {
      const updated = mkMessage({ read: true });
      db.returning.mockResolvedValueOnce([updated]);
      const result = await service.markRead(updated.id);
      expect(result.read).toBe(true);
    });

    it('throw NotFoundException si id absent', async () => {
      db.returning.mockResolvedValueOnce([]);
      await expect(service.markRead('nope')).rejects.toThrow(
        NotFoundException,
      );
    });
  });

  describe('remove', () => {
    it('supprime la row sans erreur si trouvée', async () => {
      db.returning.mockResolvedValueOnce([{ id: 'an-id' }]);
      await expect(service.remove('an-id')).resolves.toBeUndefined();
    });

    it('throw NotFoundException si id absent', async () => {
      db.returning.mockResolvedValueOnce([]);
      await expect(service.remove('nope')).rejects.toThrow(
        NotFoundException,
      );
    });
  });
});
```

- [ ] **Step 2: Lancer les tests pour vérifier qu'ils échouent**

```bash
pnpm test src/contact/contact.service.spec.ts
```

Expected: erreur `Cannot find module './contact.service'`. Normal.

- [ ] **Step 3: Créer le service**

Créer `src/contact/contact.service.ts` :

```typescript
import { resolve } from 'node:path';
import {
  Inject,
  Injectable,
  Logger,
  NotFoundException,
} from '@nestjs/common';
import { desc, eq, sql } from 'drizzle-orm';
import { DRIZZLE } from '../database/drizzle.constants';
import type { Database } from '../database/drizzle.types';
import {
  contactMessages,
  type ContactMessage,
} from '../database/schema/contact-messages';
import { AppConfigService } from '../config/app-config.service';
import { MailerService } from '../mailer/mailer.service';
import { loadTemplate, renderTemplate } from '../mailer/mailer.utils';
import {
  type PaginatedResult,
  type PaginationParams,
} from '../common/pagination';
import { CreateContactMessageDto } from './dto/create-contact-message.dto';

@Injectable()
export class ContactService {
  private readonly logger = new Logger(ContactService.name);
  private static readonly TEMPLATES_DIR = resolve(
    __dirname,
    'mail-templates',
  );

  constructor(
    @Inject(DRIZZLE) private readonly db: Database,
    private readonly cfg: AppConfigService,
    private readonly mailer: MailerService,
  ) {}

  async create(dto: CreateContactMessageDto): Promise<ContactMessage> {
    const [row] = await this.db
      .insert(contactMessages)
      .values(dto)
      .returning();

    // Fire-and-forget : la persistence est garantie, les mails sont best-effort.
    // Si SMTP down, le message est en DB et l'admin pourra le voir dans son panel.
    this.sendNotificationMails(row).catch((err: unknown) => {
      this.logger.error(
        `Failed to send contact mails for ${row.id}`,
        err instanceof Error ? err.stack : String(err),
      );
    });

    return row;
  }

  async findAll(
    params: PaginationParams,
  ): Promise<PaginatedResult<ContactMessage>> {
    const [data, totalRow] = await Promise.all([
      this.db
        .select()
        .from(contactMessages)
        .orderBy(desc(contactMessages.createdAt))
        .limit(params.limit)
        .offset(params.offset),
      this.db
        .select({ count: sql<number>`count(*)::int` })
        .from(contactMessages),
    ]);
    return {
      data,
      total: totalRow[0]?.count ?? 0,
      page: params.page,
      limit: params.limit,
    };
  }

  async unreadCount(): Promise<{ count: number }> {
    const [row] = await this.db
      .select({ count: sql<number>`count(*)::int` })
      .from(contactMessages)
      .where(eq(contactMessages.read, false));
    return { count: row?.count ?? 0 };
  }

  async markRead(id: string): Promise<ContactMessage> {
    const [row] = await this.db
      .update(contactMessages)
      .set({ read: true })
      .where(eq(contactMessages.id, id))
      .returning();
    if (!row) throw new NotFoundException(`Contact message ${id} not found`);
    return row;
  }

  async remove(id: string): Promise<void> {
    const rows = await this.db
      .delete(contactMessages)
      .where(eq(contactMessages.id, id))
      .returning({ id: contactMessages.id });
    if (rows.length === 0)
      throw new NotFoundException(`Contact message ${id} not found`);
  }

  // Helper privé non-bloquant. Les erreurs sont catch chez l'appelant.
  private async sendNotificationMails(msg: ContactMessage): Promise<void> {
    const adminTpl = loadTemplate(
      resolve(ContactService.TEMPLATES_DIR, 'contact-notification.html'),
    );
    const visitorTpl = loadTemplate(
      resolve(ContactService.TEMPLATES_DIR, 'contact-confirmation.html'),
    );
    const variables = {
      name: msg.name,
      email: msg.email,
      subject: msg.subject,
      message: msg.message,
    };
    await Promise.all([
      this.mailer.sendMail({
        to: this.cfg.contactEmail,
        subject: `Nouveau message de contact: ${msg.subject}`,
        html: renderTemplate(adminTpl, variables),
      }),
      this.mailer.sendMail({
        to: msg.email,
        subject: 'Confirmation de votre message',
        html: renderTemplate(visitorTpl, variables),
      }),
    ]);
  }
}
```

- [ ] **Step 4: Lancer les tests pour vérifier qu'ils passent**

```bash
pnpm test src/contact/contact.service.spec.ts
```

Expected: 13 tests verts.

> **Si certains tests échouent** : la nature fire-and-forget des mails complique le timing. Tous les tests utilisent `await new Promise(resolve => setImmediate(resolve))` pour drainer la microtask queue. Si un test fail toujours, augmenter à `await new Promise(resolve => setTimeout(resolve, 10))`. Ne PAS modifier le service production pour faire passer les tests.

- [ ] **Step 5: Vérifier la suite complète**

```bash
pnpm test
```

Expected: ~195 tests verts (~182 + 13 new).

- [ ] **Step 6: Lint**

```bash
pnpm lint
```

Expected: clean.

- [ ] **Step 7: Commit**

```bash
git add src/contact/contact.service.ts src/contact/contact.service.spec.ts
git commit -m "feat(contact): ContactService (CRUD + fire-and-forget mails) + 13 tests"
```

---

## Task 7: Controller + Module + AppModule wiring

**Files:**
- Create: `src/contact/contact.controller.ts`
- Create: `src/contact/contact.module.ts`
- Modify: `src/app.module.ts`

- [ ] **Step 1: Créer le controller**

Créer `src/contact/contact.controller.ts` :

```typescript
import {
  Body,
  Controller,
  Delete,
  Get,
  HttpCode,
  HttpStatus,
  Param,
  ParseUUIDPipe,
  Patch,
  Post,
  Query,
  UseGuards,
} from '@nestjs/common';
import { Throttle } from '@nestjs/throttler';
import {
  ApiBearerAuth,
  ApiOperation,
  ApiResponse,
  ApiTags,
} from '@nestjs/swagger';
import { JwtAuthGuard } from '../auth/jwt-auth.guard';
import { AppConfigService } from '../config/app-config.service';
import { parsePagination } from '../common/pagination';
import { ContactService } from './contact.service';
import { CreateContactMessageDto } from './dto/create-contact-message.dto';
import { ListContactMessagesDto } from './dto/list-contact-messages.dto';

@ApiTags('Contact')
@Controller('contact')
export class ContactController {
  constructor(
    private readonly contact: ContactService,
    private readonly cfg: AppConfigService,
  ) {}

  @Get('info')
  @ApiOperation({
    summary: 'Get public contact info from env (email, phone, location)',
  })
  getInfo() {
    return {
      email: this.cfg.contactEmail,
      phone: this.cfg.contactPhone,
      location: this.cfg.contactLocation,
    };
  }

  @Throttle({ default: { limit: 5, ttl: 60_000 } })
  @Post('messages')
  @HttpCode(HttpStatus.CREATED)
  @ApiOperation({
    summary: 'Submit a contact message (public, rate-limited 5/60s par IP)',
  })
  @ApiResponse({ status: 201, description: 'Message saved' })
  @ApiResponse({ status: 400, description: 'Validation error' })
  @ApiResponse({ status: 429, description: 'Too many requests' })
  create(@Body() dto: CreateContactMessageDto) {
    return this.contact.create(dto);
  }

  @UseGuards(JwtAuthGuard)
  @Get('messages')
  @ApiBearerAuth()
  @ApiOperation({
    summary:
      'List contact messages (admin, paginated, sorted createdAt DESC)',
  })
  findAll(@Query() query: ListContactMessagesDto) {
    return this.contact.findAll(parsePagination(query));
  }

  @UseGuards(JwtAuthGuard)
  @Get('messages/unread-count')
  @ApiBearerAuth()
  @ApiOperation({ summary: 'Count unread messages (admin)' })
  unreadCount() {
    return this.contact.unreadCount();
  }

  @UseGuards(JwtAuthGuard)
  @Patch('messages/:id/read')
  @ApiBearerAuth()
  @ApiOperation({ summary: 'Mark a message as read (admin)' })
  @ApiResponse({ status: 404, description: 'Not found' })
  markRead(@Param('id', ParseUUIDPipe) id: string) {
    return this.contact.markRead(id);
  }

  @UseGuards(JwtAuthGuard)
  @Delete('messages/:id')
  @ApiBearerAuth()
  @HttpCode(HttpStatus.NO_CONTENT)
  @ApiOperation({ summary: 'Delete a message (admin)' })
  @ApiResponse({ status: 404, description: 'Not found' })
  remove(@Param('id', ParseUUIDPipe) id: string) {
    return this.contact.remove(id);
  }
}
```

> **Ordre des routes critique** : `Get('messages/unread-count')` est déclaré AVANT `Patch('messages/:id/read')` pour que NestJS ne matche pas `:id = 'unread-count'`. NestJS résout dans l'ordre déclaré.

- [ ] **Step 2: Créer le module**

Créer `src/contact/contact.module.ts` :

```typescript
import { Module } from '@nestjs/common';
import { AuthModule } from '../auth/auth.module';
import { ContactController } from './contact.controller';
import { ContactService } from './contact.service';

@Module({
  imports: [AuthModule],
  controllers: [ContactController],
  providers: [ContactService],
})
export class ContactModule {}
```

- [ ] **Step 3: Wirer dans `src/app.module.ts`**

Ouvrir `src/app.module.ts`. État actuel — la section des imports en haut :

```typescript
import { ProjectsModule } from './projects/projects.module';
import { MailerModule } from './mailer/mailer.module';
```

Ajouter immédiatement après ces lignes :

```typescript
import { ThrottlerModule, ThrottlerGuard } from '@nestjs/throttler';
import { APP_GUARD } from '@nestjs/core';
import { ContactModule } from './contact/contact.module';
```

État actuel du `imports` array (se termine par `MailerModule`) :

```typescript
    StorageModule,
    ProjectsModule,
    MailerModule,
  ],
```

Le remplacer par :

```typescript
    StorageModule,
    ProjectsModule,
    MailerModule,
    ThrottlerModule.forRoot([{ ttl: 60_000, limit: 10 }]),
    ContactModule,
  ],
```

État actuel du `@Module({...})` decorator — il devrait se terminer par juste `imports: [...]` sans `providers:`. Vérifier en lisant le fichier ; si c'est le cas, ajouter un `providers:` array. Sinon, étendre le `providers:` existant.

Ajouter dans le `@Module({})` decorator (après `imports`) :

```typescript
  providers: [
    {
      provide: APP_GUARD,
      useClass: ThrottlerGuard,
    },
  ],
```

Le `@Module({})` final ressemblera à :

```typescript
@Module({
  imports: [
    // ... existing
    StorageModule,
    ProjectsModule,
    MailerModule,
    ThrottlerModule.forRoot([{ ttl: 60_000, limit: 10 }]),
    ContactModule,
  ],
  providers: [
    {
      provide: APP_GUARD,
      useClass: ThrottlerGuard,
    },
  ],
})
export class AppModule {}
```

- [ ] **Step 4: Vérifier que TypeScript compile**

```bash
pnpm build
```

Expected: clean.

- [ ] **Step 5: Suite de tests**

```bash
pnpm test
```

Expected: ~195 tests verts (idem fin Task 6 — pas de nouveau test ici).

- [ ] **Step 6: Smoke test boot**

S'assurer que Postgres + MinIO + Mailpit tournent :

```bash
pnpm db:up && pnpm db:wait
pnpm s3:up
pnpm mail:up
sleep 3
```

Démarrer l'app avec timeout :

```bash
timeout 25 pnpm dev > /tmp/contact-boot.log 2>&1 || true
grep -E 'ContactModule|ThrottlerModule|/contact' /tmp/contact-boot.log
```

Expected (au moins ces lignes) :
- `ContactModule dependencies initialized`
- `ThrottlerModule dependencies initialized`
- `Mapped {/contact/info, GET}` route
- `Mapped {/contact/messages, POST}` route
- `Mapped {/contact/messages, GET}` route
- `Mapped {/contact/messages/unread-count, GET}` route
- `Mapped {/contact/messages/:id/read, PATCH}` route
- `Mapped {/contact/messages/:id, DELETE}` route
- `Nest application successfully started`

Si n'importe quelle route est manquante, c'est un bug — STOP et reporter.

- [ ] **Step 7: Lint**

```bash
pnpm lint
```

Expected: clean.

- [ ] **Step 8: Commit**

```bash
git add src/contact/contact.controller.ts src/contact/contact.module.ts src/app.module.ts
git commit -m "feat(contact): controller (6 endpoints) + module + ThrottlerModule wiring"
```

---

## Task 8: Vérification e2e manuelle

**Files:** Aucun changement de code. Cette tâche valide tout le flow contre Postgres + MinIO + Mailpit réels.

**Préparation** : avoir les containers up (Task 7 step 6 les a démarrés).

- [ ] **Step 1: Démarrer la stack et l'app**

```bash
pnpm db:up && pnpm db:wait && pnpm s3:up && pnpm mail:up
sleep 3
nohup pnpm dev > /tmp/contact-e2e-dev.log 2>&1 &
echo $! > /tmp/contact-e2e-pid
sleep 8
```

Vérifier que l'app répond :

```bash
curl -fsS http://localhost:3000/health
```

Expected: `{"status":"ok"}` ou `{"status":"degraded"}`.

- [ ] **Step 2: Vider Mailpit (optionnel mais propre)**

```bash
curl -X DELETE http://localhost:8025/api/v1/messages
```

- [ ] **Step 3: GET /contact/info (public)**

```bash
curl -s http://localhost:3000/contact/info
```

Expected: JSON `{ email, phone, location }` avec les valeurs de `.env` (`admin@nedellec-julien.fr`, `+33 6 00 00 00 00`, `Lyon, France`).

- [ ] **Step 4: POST /contact/messages (public)**

```bash
curl -X POST http://localhost:3000/contact/messages \
  -H 'Content-Type: application/json' \
  -d '{"name":"Visiteur Test","email":"visitor@test.com","subject":"Test E2E","message":"Hello from e2e test."}' -i
```

Expected: HTTP 201, body retourne le row complet (id, createdAt, etc.).

Noter l'`id` du message créé pour les steps suivants.

- [ ] **Step 5: Vérifier que les 2 mails sont dans Mailpit**

```bash
sleep 2
curl -s 'http://localhost:8025/api/v1/messages?limit=10' | python3 -c '
import sys, json
data = json.load(sys.stdin)
print(f"Total: {data.get(\"total\", 0)}")
for m in data.get("messages", []):
    print(f"  - {m[\"To\"][0][\"Address\"]} | {m[\"Subject\"]}")'
```

Expected: 2 messages :
- `admin@nedellec-julien.fr | Nouveau message de contact: Test E2E`
- `visitor@test.com | Confirmation de votre message`

Si seulement 1 mail ou 0, attendre encore 2-3s et retry — les mails sont fire-and-forget.

- [ ] **Step 6: Tester le throttling (5/60s)**

Spam 7 requêtes en moins d'une minute :

```bash
for i in $(seq 1 7); do
  STATUS=$(curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:3000/contact/messages \
    -H 'Content-Type: application/json' \
    -d "{\"name\":\"Spam $i\",\"email\":\"spam@test.com\",\"subject\":\"Spam\",\"message\":\"$i\"}")
  echo "Request $i: HTTP $STATUS"
done
```

Expected: les 5 premières renvoient 201, les 2 suivantes (6 et 7) renvoient 429.

- [ ] **Step 7: GET /contact/messages (admin)**

D'abord login pour obtenir le cookie :

```bash
curl -X POST http://localhost:3000/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"email":"admin@nedellec-julien.fr","password":"change-me-please-at-least-12-chars"}' \
  -c /tmp/contact-cookies.txt
```

Expected: HTTP 200 avec Set-Cookie.

Lister les messages :

```bash
curl -s -b /tmp/contact-cookies.txt 'http://localhost:3000/contact/messages?page=1&limit=10' | python3 -m json.tool | head -30
```

Expected: JSON `{ data, total, page, limit }` avec ≥ 5 messages (les non-throttlés du step 6 + ceux du step 4).

- [ ] **Step 8: GET /contact/messages/unread-count**

```bash
curl -s -b /tmp/contact-cookies.txt http://localhost:3000/contact/messages/unread-count
```

Expected: `{"count":N}` où N = nombre de messages non-lus.

- [ ] **Step 9: PATCH /contact/messages/:id/read**

Récupérer un id depuis la liste :

```bash
MSG_ID=$(curl -s -b /tmp/contact-cookies.txt http://localhost:3000/contact/messages | python3 -c 'import sys, json; print(json.load(sys.stdin)["data"][0]["id"])')
echo "Message ID: $MSG_ID"
curl -s -X PATCH -b /tmp/contact-cookies.txt http://localhost:3000/contact/messages/$MSG_ID/read | python3 -m json.tool
```

Expected: row retournée avec `"read": true`.

Vérifier que `unread-count` a baissé :

```bash
curl -s -b /tmp/contact-cookies.txt http://localhost:3000/contact/messages/unread-count
```

Expected: count diminué de 1.

- [ ] **Step 10: PATCH avec id inexistant → 404**

```bash
curl -s -o /dev/null -w "%{http_code}\n" -X PATCH -b /tmp/contact-cookies.txt \
  http://localhost:3000/contact/messages/00000000-0000-0000-0000-000000000000/read
```

Expected: `404`.

- [ ] **Step 11: DELETE /contact/messages/:id**

```bash
curl -s -o /dev/null -w "%{http_code}\n" -X DELETE -b /tmp/contact-cookies.txt \
  http://localhost:3000/contact/messages/$MSG_ID
```

Expected: `204`.

Vérifier qu'il n'existe plus :

```bash
curl -s -o /dev/null -w "%{http_code}\n" -X PATCH -b /tmp/contact-cookies.txt \
  http://localhost:3000/contact/messages/$MSG_ID/read
```

Expected: `404`.

- [ ] **Step 12: Sans auth → 401**

```bash
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3000/contact/messages
```

Expected: `401`.

- [ ] **Step 13: DTO invalide → 400**

```bash
curl -s -o /dev/null -w "%{http_code}\n" -X POST http://localhost:3000/contact/messages \
  -H 'Content-Type: application/json' \
  -d '{"name":"","email":"not-email","subject":"","message":""}'
```

Expected: `400` (multiple validations échouent).

- [ ] **Step 14: Cleanup**

```bash
kill $(cat /tmp/contact-e2e-pid) 2>/dev/null
sleep 1
rm /tmp/contact-cookies.txt /tmp/contact-e2e-pid /tmp/contact-e2e-dev.log /tmp/contact-boot.log 2>/dev/null
```

- [ ] **Step 15: Pas de commit**

Cette tâche ne modifie pas de code. Si tous les steps passent, marquer comme done.

---

## Task 9: README + clôture

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Ajouter la section Contact**

Ouvrir `README.md`. Localiser la section `## Mailer` (ajoutée au sous-projet précédent). Insérer la nouvelle section `## Contact` **immédiatement après** la section Mailer et **avant** `## Migration depuis le backend Hono`.

Contenu à insérer :

```markdown
## Contact

Module métier pour le formulaire de contact public et la gestion admin des messages reçus. **Premier consommateur réel du `MailerService`** livré au sous-projet précédent.

**Schéma** : table `contact_message` (uuid + 6 cols + 3 indexes : `read`, `createdAt`, composite `read+createdAt`).

**6 endpoints sous `/contact`** :

| Méthode | Chemin | Auth | Rôle |
|---|---|---|---|
| GET | `/contact/info` | ❌ | Retourne `{ email, phone, location }` depuis env vars (`CONTACT_*`). |
| POST | `/contact/messages` | ❌ | Soumission publique du formulaire. **Throttle 5/60s par IP**. Insère en DB + envoie 2 mails fire-and-forget (notification admin + confirmation visiteur). Retourne 201 avec la row. |
| GET | `/contact/messages` | ✅ | Liste paginée (`?page&limit`, defaults 1/10, max limit 100), tri `createdAt DESC`. |
| GET | `/contact/messages/unread-count` | ✅ | Compteur de messages non-lus. |
| PATCH | `/contact/messages/:id/read` | ✅ | Marque le message comme lu. |
| DELETE | `/contact/messages/:id` | ✅ | Suppression hard. |

**Throttling** : `@nestjs/throttler` configuré globalement (10/60s par défaut), override fin sur `POST /messages` à 5/60s. Guard global via `APP_GUARD`. Storage in-memory — switcher Redis si scale horizontal.

**Stratégie mails** : fire-and-forget après `db.insert`. Le visiteur reçoit 201 dès que la persistence est garantie. Les 2 mails partent en arrière-plan. En cas d'échec SMTP (3 retries du `MailerService` épuisés), l'erreur est loggée mais le visiteur ne voit rien — le message reste en DB pour consultation admin.

**Templates** :
- `src/contact/mail-templates/contact-notification.html` — envoyé à `CONTACT_EMAIL` (admin)
- `src/contact/mail-templates/contact-confirmation.html` — envoyé à l'email du visiteur

Variables : `{{name}}`, `{{email}}`, `{{subject}}`, `{{message}}`.

**Helpers transversaux** :
- `src/common/pagination.ts` : `parsePagination` + types `PaginationParams`/`PaginatedResult<T>`. Premier utilitaire de `src/common/`, réutilisable par futurs modules paginés (Bookings, etc.).

**Configuration prod** :

```bash
CONTACT_EMAIL=admin@nedellec-julien.fr  # destinataire des notifications
CONTACT_PHONE=+33 6 00 00 00 00
CONTACT_LOCATION=Lyon, France
```

Le `SMTP_FROM` (sous-projet Mailer) reste l'expéditeur — souvent identique à `CONTACT_EMAIL` mais conceptuellement distinct.

**Voir le spec complet** : [`docs/superpowers/specs/2026-04-26-contact-design.md`](docs/superpowers/specs/2026-04-26-contact-design.md).
```

- [ ] **Step 2: Mettre à jour la liste des sous-projets**

Localiser la liste numérotée dans la section `## Migration depuis le backend Hono`. État actuel après Mailer (11 items) :

```markdown
1. ✅ Fondations
2. ✅ Auth (Users + JWT + 2FA + backup codes)
3. ✅ Profile public (Profile, Hero, SocialLinks, Diplomas, Technologies, Expertises, ServicePricing)
4. ✅ S3 Storage (StorageModule + MinIO local + Garage prod)
5. ✅ Projects (CRUD + upload image qui consomme S3 Storage)
6. ✅ Avatar Profile (`POST /profile/avatar` + transformation key→URL en sortie API, cohérent Projects)
7. ✅ Mailer (MailerModule @Global + Mailpit local + nodemailer)
8. **Contact** *(prochain)* (messages + 2 templates qui consomment Mailer)
9. Bookings (réservations + slots + 2 templates qui consomment Mailer)
10. CV (upload S3 + download)
11. Analytics (page views + agrégats)
```

Le remplacer par (item 8 marqué ✅, item 9 marqué prochain) :

```markdown
1. ✅ Fondations
2. ✅ Auth (Users + JWT + 2FA + backup codes)
3. ✅ Profile public (Profile, Hero, SocialLinks, Diplomas, Technologies, Expertises, ServicePricing)
4. ✅ S3 Storage (StorageModule + MinIO local + Garage prod)
5. ✅ Projects (CRUD + upload image qui consomme S3 Storage)
6. ✅ Avatar Profile (`POST /profile/avatar` + transformation key→URL en sortie API, cohérent Projects)
7. ✅ Mailer (MailerModule @Global + Mailpit local + nodemailer)
8. ✅ Contact (6 endpoints + 2 templates + throttling 5/60s, premier consumer Mailer)
9. **Bookings** *(prochain)* (réservations + slots + 2 templates qui consomment Mailer)
10. CV (upload S3 + download)
11. Analytics (page views + agrégats)
```

- [ ] **Step 3: Sanity check**

Relire visuellement les sections modifiées :
- La section `## Contact` est bien entre `## Mailer` et `## Migration depuis le backend Hono`.
- Le tableau d'endpoints rend correctement.
- Le lien spec pointe vers `2026-04-26-contact-design.md`.
- Item 8 est ✅ Contact, item 9 est Bookings en cours.

- [ ] **Step 4: Final check global**

```bash
pnpm lint && pnpm build && pnpm test
```

Expected: tout passe. ~195 tests verts.

- [ ] **Step 5: Vérifier les fichiers du sous-projet**

```bash
git diff --stat HEAD~7..HEAD
```

Expected (à 1-2 fichiers près selon l'ordre des commits) :
```
 .env.example                                                    |   5 +
 README.md                                                       | ~50 +-
 drizzle/0003_*.sql                                              | new
 nest-cli.json                                                   |   6 +-
 package.json                                                    |   3 +-
 pnpm-lock.yaml                                                  | many +-
 src/app.module.ts                                               |  10 +-
 src/common/pagination.ts                                        | new
 src/common/pagination.spec.ts                                   | new
 src/config/app-config.service.ts                                |   9 +
 src/config/env.schema.ts                                        |   5 +
 src/config/env.validation.spec.ts                               |  20 +-
 src/contact/contact.controller.ts                               | new
 src/contact/contact.module.ts                                   | new
 src/contact/contact.service.ts                                  | new
 src/contact/contact.service.spec.ts                             | new
 src/contact/dto/create-contact-message.dto.ts                   | new
 src/contact/dto/list-contact-messages.dto.ts                    | new
 src/contact/mail-templates/contact-confirmation.html            | new
 src/contact/mail-templates/contact-notification.html            | new
 src/database/schema/contact-messages.ts                         | new
 src/database/schema/index.ts                                    |   3 +
```

- [ ] **Step 6: Commit**

```bash
git add README.md
git commit -m "docs: README — section Contact + liste sous-projets"
```

---

## Critères de done globaux (rappel du spec § 16)

Le sous-projet est terminé quand :

1. ✅ Schéma + migration `0003` appliquée (Task 3 step 8 vérifié).
2. ✅ `@nestjs/throttler` installé, `ThrottlerModule` + `APP_GUARD` configurés (Task 1, 7).
3. ✅ 3 env vars `CONTACT_*` + 3 getters fonctionnels, validation Zod fail-fast (Task 1).
4. ✅ Helper `parsePagination` partagé dans `src/common/`, testé (Task 2).
5. ✅ 6 endpoints fonctionnels (Task 8 e2e).
6. ✅ Templates HTML copiés dans `dist/` au build (Task 4 step 5).
7. ✅ ~18 nouveaux tests verts, total ~192 (Task 2 + Task 6).
8. ✅ Build prod + lint clean (Task 9 step 4).
9. ✅ Vérification e2e manuelle (Task 8).
10. ✅ README mis à jour (Task 9).
