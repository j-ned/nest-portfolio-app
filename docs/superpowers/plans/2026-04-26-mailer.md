# Mailer — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implémenter le sous-projet "Mailer" du backend NestJS (`MailerModule @Global` exposant un `MailerService` générique avec retry, container Mailpit local pour le SMTP de dev) selon le spec `2026-04-26-mailer-design.md`. ~10 nouveaux tests, total projet ~167.

**Architecture:** Module NestJS d'infrastructure pure, calque exact `StorageModule`. Token `MAIL_TRANSPORTER` (Symbol) injecté via factory créant un `Transporter` nodemailer depuis `AppConfigService`. `MailerService` exporté avec 1 méthode `sendMail({ to, subject, html })` (3 retries linear backoff). Pas de controller HTTP, pas d'interaction DB. `OnModuleDestroy` pour cleanup `transporter.close()`.

**Tech Stack:** NestJS 11, `nodemailer` (SMTP), Mailpit container (dev local SMTP catch-all + web UI), Jest + Transporter mock (tests), Podman compose.

**Référence spec :** `docs/superpowers/specs/2026-04-26-mailer-design.md`

---

## File Structure

### Fichiers à créer

| Chemin | Rôle |
|---|---|
| `src/mailer/mailer.constants.ts` | `export const MAIL_TRANSPORTER = Symbol('MAIL_TRANSPORTER')` |
| `src/mailer/mailer.utils.ts` | `renderTemplate(html, vars)` + `loadTemplate(absolutePath)` |
| `src/mailer/mailer.utils.spec.ts` | ~4 tests purs sur `renderTemplate` |
| `src/mailer/mailer.service.ts` | `sendMail()` avec retry 3x linear backoff |
| `src/mailer/mailer.service.spec.ts` | ~6 tests via mock du `Transporter` |
| `src/mailer/mailer.module.ts` | `@Global`, providers `MAIL_TRANSPORTER` + `MailerService`, `OnModuleDestroy` |

### Fichiers à modifier

| Chemin | Modification |
|---|---|
| `package.json` | +`nodemailer` (prod), +`@types/nodemailer` (dev), +4 scripts `mail:*`, modif `predev` |
| `pnpm-lock.yaml` | Auto par pnpm |
| `src/config/env.schema.ts` | +6 env vars `SMTP_*` |
| `src/config/env.validation.spec.ts` | +tests pour les nouvelles vars |
| `src/config/app-config.service.ts` | +6 getters `smtp*` |
| `.env.example` | +6 env vars documentées |
| `compose.yaml` | +service `mailpit` |
| `src/app.module.ts` | +`MailerModule` import |
| `README.md` | +section `## Mailer` + liste sous-projets renumérotée |

---

## Task 1: Install deps + extension env validation

**Files:**
- Modify: `package.json`, `pnpm-lock.yaml`
- Modify: `src/config/env.schema.ts`
- Modify: `src/config/env.validation.spec.ts`
- Modify: `src/config/app-config.service.ts`
- Modify: `.env.example`

- [ ] **Step 1: Installer la dep prod**

```bash
pnpm add nodemailer
```

Expected: pnpm résout sans erreur, ajoute `nodemailer` dans `dependencies`.

- [ ] **Step 2: Installer le typage de dev**

```bash
pnpm add -D @types/nodemailer
```

Expected: `@types/nodemailer` ajouté dans `devDependencies`.

- [ ] **Step 3: Étendre `src/config/env.schema.ts`**

Ouvrir le fichier. État actuel (avant modification) — la fin de l'objet `envSchema` :

```typescript
  // S3 Storage
  S3_ENDPOINT: z.string().url(),
  S3_REGION: z.string().min(1),
  S3_ACCESS_KEY: z.string().min(4),
  S3_SECRET_KEY: z.string().min(8),
  S3_PUBLIC_URL: z.string().url().optional(),
});
```

Ajouter les 6 nouvelles vars SMTP juste avant la fermeture `})`, après `S3_PUBLIC_URL` :

```typescript
  // S3 Storage
  S3_ENDPOINT: z.string().url(),
  S3_REGION: z.string().min(1),
  S3_ACCESS_KEY: z.string().min(4),
  S3_SECRET_KEY: z.string().min(8),
  S3_PUBLIC_URL: z.string().url().optional(),

  // Mailer (SMTP)
  SMTP_HOST: z.string().min(1),
  SMTP_PORT: z.coerce.number().int().min(1).max(65535).default(587),
  SMTP_SECURE: z.coerce.boolean().default(false),
  SMTP_USER: z.string().min(1),
  SMTP_PASS: z.string().min(1),
  SMTP_FROM: z.string().email(),
});
```

- [ ] **Step 4: Étendre `src/config/env.validation.spec.ts`**

Ouvrir le fichier. La constante `baseValid` au début doit être étendue avec les vars SMTP requises pour que les tests existants continuent de passer (sinon Zod va rejeter sur SMTP absent). Modifier :

```typescript
  const baseValid = {
    DATABASE_URL: 'postgres://u:p@localhost:55432/db',
    JWT_SECRET: '0123456789abcdef0123456789abcdef', // exactly 32 chars
    S3_ENDPOINT: 'http://localhost:9000',
    S3_REGION: 'us-east-1',
    S3_ACCESS_KEY: 'access-key',
    S3_SECRET_KEY: 'secret-key-12345',
  };
```

Pour devenir :

```typescript
  const baseValid = {
    DATABASE_URL: 'postgres://u:p@localhost:55432/db',
    JWT_SECRET: '0123456789abcdef0123456789abcdef', // exactly 32 chars
    S3_ENDPOINT: 'http://localhost:9000',
    S3_REGION: 'us-east-1',
    S3_ACCESS_KEY: 'access-key',
    S3_SECRET_KEY: 'secret-key-12345',
    SMTP_HOST: 'localhost',
    SMTP_USER: 'mailpit',
    SMTP_PASS: 'mailpit',
    SMTP_FROM: 'noreply@test.local',
  };
```

Puis ajouter ces 4 nouveaux tests à la fin du `describe('validateEnv', ...)` (juste avant la closing `})` du fichier) :

```typescript
  it('défaut SMTP_PORT à 587', () => {
    const result = validateEnv(baseValid);
    expect(result.SMTP_PORT).toBe(587);
  });

  it('défaut SMTP_SECURE à false', () => {
    const result = validateEnv(baseValid);
    expect(result.SMTP_SECURE).toBe(false);
  });

  it('coerce SMTP_PORT depuis une string', () => {
    const result = validateEnv({ ...baseValid, SMTP_PORT: '1025' });
    expect(result.SMTP_PORT).toBe(1025);
  });

  it('rejette SMTP_FROM si pas un email valide', () => {
    expect(() =>
      validateEnv({ ...baseValid, SMTP_FROM: 'not-an-email' }),
    ).toThrow(/SMTP_FROM/);
  });
```

- [ ] **Step 5: Étendre `src/config/app-config.service.ts`**

Ouvrir le fichier. À la fin de la classe, juste après le getter `s3PublicUrl()`, ajouter les 6 nouveaux getters :

```typescript
  get smtpHost() {
    return this.config.get('SMTP_HOST', { infer: true });
  }
  get smtpPort() {
    return this.config.get('SMTP_PORT', { infer: true });
  }
  get smtpSecure() {
    return this.config.get('SMTP_SECURE', { infer: true });
  }
  get smtpUser() {
    return this.config.get('SMTP_USER', { infer: true });
  }
  get smtpPass() {
    return this.config.get('SMTP_PASS', { infer: true });
  }
  get smtpFrom() {
    return this.config.get('SMTP_FROM', { infer: true });
  }
```

- [ ] **Step 6: Étendre `.env.example`**

Ouvrir `.env.example`. Ajouter à la fin du fichier :

```bash

# Mailer — Mailpit local en dev, vrai SMTP en prod
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=mailpit
SMTP_PASS=mailpit
SMTP_FROM=noreply@nedellec-julien.fr
```

- [ ] **Step 7: Mettre à jour `.env`**

Le `.env` du dev local doit aussi contenir ces vars pour que l'app puisse booter. Ajouter à la fin de `.env` (créer le fichier s'il n'existe pas, mais il devrait exister depuis les sous-projets précédents) :

```bash

# Mailer
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=mailpit
SMTP_PASS=mailpit
SMTP_FROM=noreply@nedellec-julien.fr
```

- [ ] **Step 8: Vérifier les tests env**

```bash
pnpm test src/config/env.validation.spec.ts
```

Expected: tous verts (existants + 4 nouveaux). Si un test existant échoue, c'est probablement que `baseValid` n'a pas été correctement étendu — vérifier.

- [ ] **Step 9: Vérifier la suite globale**

```bash
pnpm test
```

Expected: tous verts (~157 + 4 = ~161).

- [ ] **Step 10: Vérifier le build**

```bash
pnpm build
```

Expected: clean.

- [ ] **Step 11: Lint**

```bash
pnpm lint
```

Expected: clean.

- [ ] **Step 12: Commit**

```bash
git add package.json pnpm-lock.yaml src/config/env.schema.ts src/config/env.validation.spec.ts src/config/app-config.service.ts .env.example .env
git commit -m "feat(deps,config): nodemailer + extension env validation SMTP"
```

> Note : le `.env` est volontairement commité parce qu'il l'est déjà depuis les sous-projets précédents (calque la convention du dépôt — voir `git log -- .env`). Si ce n'est pas le cas chez toi, retire `.env` du `git add`.

---

## Task 2: Compose Mailpit + scripts pnpm

**Files:**
- Modify: `compose.yaml`
- Modify: `package.json`

- [ ] **Step 1: Étendre `compose.yaml`**

Ouvrir le fichier. État actuel : 3 services (`postgres`, `minio`, `minio-init`) suivis de la section `volumes:`.

Insérer le nouveau service `mailpit` après `minio-init` et avant `volumes:` :

```yaml
  mailpit:
    image: docker.io/axllent/mailpit:latest
    container_name: portfolio-nest-mail
    restart: unless-stopped
    ports:
      - "1025:1025"
      - "8025:8025"
    environment:
      MP_SMTP_AUTH_ACCEPT_ANY: 1
      MP_SMTP_AUTH_ALLOW_INSECURE: 1
    healthcheck:
      test: ["CMD", "wget", "-q", "-O-", "http://localhost:8025"]
      interval: 5s
      timeout: 3s
      retries: 5
```

(Pas de volume — Mailpit est éphémère en dev.)

- [ ] **Step 2: Étendre `package.json` — scripts mail:***

Ouvrir `package.json`. Localiser la section `scripts`. Après les 5 scripts `s3:*` (`s3:up`, `s3:down`, `s3:logs`, `s3:console`, `s3:reset`), ajouter ces 4 scripts :

```json
"mail:up": "podman compose up -d mailpit",
"mail:down": "podman compose stop mailpit",
"mail:logs": "podman compose logs -f mailpit",
"mail:console": "echo 'Mailpit web UI: http://localhost:8025'",
```

- [ ] **Step 3: Étendre le script `predev`**

Toujours dans `package.json`. Le script `predev` actuel est :

```json
"predev": "pnpm db:up && pnpm db:wait && pnpm s3:up",
```

Le remplacer par :

```json
"predev": "pnpm db:up && pnpm db:wait && pnpm s3:up && pnpm mail:up",
```

- [ ] **Step 4: Tester `mail:up`**

```bash
pnpm mail:up
```

Expected: Podman télécharge l'image (première fois) et démarre le container. La sortie doit montrer `Container portfolio-nest-mail Started` ou similaire.

- [ ] **Step 5: Vérifier que Mailpit répond**

```bash
sleep 3
curl -fsS http://localhost:8025 | head -20
```

Expected: HTML de la page Mailpit (titre `<title>Mailpit</title>` ou similaire).

Si le container met du temps à démarrer, attendre quelques secondes de plus et réessayer.

- [ ] **Step 6: Vérifier le port SMTP**

```bash
nc -zv localhost 1025 2>&1
```

Expected: `Connection to localhost (127.0.0.1) 1025 port [tcp/*] succeeded!` ou équivalent.

(Si `nc` n'est pas dispo, ce step est optionnel — la vérif principale est step 5.)

- [ ] **Step 7: Commit**

```bash
git add compose.yaml package.json
git commit -m "feat(compose): mailpit container + scripts mail:* + predev étendu"
```

---

## Task 3: Mailer utils + tests TDD

**Files:**
- Create: `src/mailer/mailer.utils.ts`
- Create: `src/mailer/mailer.utils.spec.ts`

- [ ] **Step 1: Écrire les tests d'abord (TDD)**

Créer `src/mailer/mailer.utils.spec.ts` :

```typescript
import { renderTemplate } from './mailer.utils';

describe('renderTemplate', () => {
  it('remplace une variable simple', () => {
    expect(renderTemplate('Hello {{name}}', { name: 'Julien' })).toBe(
      'Hello Julien',
    );
  });

  it('remplace plusieurs occurrences de la même variable', () => {
    expect(renderTemplate('{{x}} et encore {{x}}', { x: 'A' })).toBe(
      'A et encore A',
    );
  });

  it('laisse intactes les variables non fournies', () => {
    expect(renderTemplate('Hi {{a}} {{b}}', { a: 'X' })).toBe('Hi X {{b}}');
  });

  it('gère un template sans variables ni placeholders', () => {
    expect(renderTemplate('static', {})).toBe('static');
  });
});
```

> Note : on ne teste pas `loadTemplate` (wrapper trivial sur `readFileSync`). Couverture implicite par les tests d'intégration au sous-projet Contact.

- [ ] **Step 2: Lancer les tests pour vérifier qu'ils échouent**

```bash
pnpm test src/mailer/mailer.utils.spec.ts
```

Expected: erreur `Cannot find module './mailer.utils'`. Normal, on n'a pas encore créé l'implémentation.

- [ ] **Step 3: Créer l'implémentation**

Créer `src/mailer/mailer.utils.ts` :

```typescript
import { readFileSync } from 'node:fs';

export function renderTemplate(
  html: string,
  variables: Record<string, string>,
): string {
  let result = html;
  for (const [key, value] of Object.entries(variables)) {
    result = result.replaceAll(`{{${key}}}`, value);
  }
  return result;
}

export function loadTemplate(absolutePath: string): string {
  return readFileSync(absolutePath, 'utf-8');
}
```

- [ ] **Step 4: Lancer les tests pour vérifier qu'ils passent**

```bash
pnpm test src/mailer/mailer.utils.spec.ts
```

Expected: 4 tests verts.

- [ ] **Step 5: Lint**

```bash
pnpm lint
```

Expected: clean.

- [ ] **Step 6: Commit**

```bash
git add src/mailer/mailer.utils.ts src/mailer/mailer.utils.spec.ts
git commit -m "feat(mailer): utils renderTemplate + loadTemplate + tests"
```

---

## Task 4: MailerService + tests TDD

**Files:**
- Create: `src/mailer/mailer.constants.ts`
- Create: `src/mailer/mailer.service.ts`
- Create: `src/mailer/mailer.service.spec.ts`

- [ ] **Step 1: Créer le fichier de constantes**

Créer `src/mailer/mailer.constants.ts` :

```typescript
export const MAIL_TRANSPORTER = Symbol('MAIL_TRANSPORTER');
```

- [ ] **Step 2: Écrire les tests d'abord (TDD)**

Créer `src/mailer/mailer.service.spec.ts` :

```typescript
/* eslint-disable @typescript-eslint/unbound-method */
import { Test, TestingModule } from '@nestjs/testing';
import type { Transporter } from 'nodemailer';
import { MailerService } from './mailer.service';
import { MAIL_TRANSPORTER } from './mailer.constants';
import { AppConfigService } from '../config/app-config.service';

describe('MailerService', () => {
  let service: MailerService;
  let transporter: { sendMail: jest.Mock };
  let cfg: { smtpFrom: string };

  beforeEach(async () => {
    transporter = { sendMail: jest.fn() };
    cfg = { smtpFrom: 'noreply@test.local' };

    const module: TestingModule = await Test.createTestingModule({
      providers: [
        MailerService,
        { provide: MAIL_TRANSPORTER, useValue: transporter as unknown as Transporter },
        { provide: AppConfigService, useValue: cfg as unknown as AppConfigService },
      ],
    }).compile();
    service = module.get(MailerService);
  });

  it('envoie le mail avec les bons paramètres', async () => {
    transporter.sendMail.mockResolvedValueOnce({});
    await service.sendMail({
      to: 'visitor@example.com',
      subject: 'Hello',
      html: '<p>hi</p>',
    });
    expect(transporter.sendMail).toHaveBeenCalledTimes(1);
    expect(transporter.sendMail).toHaveBeenCalledWith({
      from: 'noreply@test.local',
      to: 'visitor@example.com',
      subject: 'Hello',
      html: '<p>hi</p>',
    });
  });

  it('utilise cfg.smtpFrom comme expéditeur', async () => {
    transporter.sendMail.mockResolvedValueOnce({});
    await service.sendMail({ to: 'x@y.com', subject: 's', html: 'h' });
    const args = transporter.sendMail.mock.calls[0][0];
    expect(args.from).toBe('noreply@test.local');
  });

  it('retry une fois si la première tentative échoue, puis succès', async () => {
    transporter.sendMail
      .mockRejectedValueOnce(new Error('transient'))
      .mockResolvedValueOnce({});
    jest.useFakeTimers();
    const promise = service.sendMail({
      to: 'x@y.com',
      subject: 's',
      html: 'h',
    });
    // Avancer le temps pour le backoff (1000ms après tentative 1)
    await Promise.resolve(); // laisse la première tentative échouer
    await jest.advanceTimersByTimeAsync(1000);
    await promise;
    jest.useRealTimers();
    expect(transporter.sendMail).toHaveBeenCalledTimes(2);
  });

  it('throw après 3 tentatives échouées', async () => {
    const err = new Error('persistent');
    transporter.sendMail.mockRejectedValue(err);
    jest.useFakeTimers();
    const promise = service.sendMail({
      to: 'x@y.com',
      subject: 's',
      html: 'h',
    });
    // Avance le temps pour les 2 backoffs (1s + 2s)
    await Promise.resolve();
    await jest.advanceTimersByTimeAsync(1000);
    await Promise.resolve();
    await jest.advanceTimersByTimeAsync(2000);
    await expect(promise).rejects.toThrow('persistent');
    jest.useRealTimers();
    expect(transporter.sendMail).toHaveBeenCalledTimes(3);
  });

  it('ne retry pas si la première tentative réussit', async () => {
    transporter.sendMail.mockResolvedValueOnce({});
    await service.sendMail({ to: 'x@y.com', subject: 's', html: 'h' });
    expect(transporter.sendMail).toHaveBeenCalledTimes(1);
  });

  it('passe le sujet et le HTML tels quels (pas de transformation)', async () => {
    transporter.sendMail.mockResolvedValueOnce({});
    const html = '<html><body><h1>Hello {{name}}</h1></body></html>';
    await service.sendMail({
      to: 'x@y.com',
      subject: 'Sujet avec accents éàç',
      html,
    });
    const args = transporter.sendMail.mock.calls[0][0];
    expect(args.subject).toBe('Sujet avec accents éàç');
    expect(args.html).toBe(html);
  });
});
```

- [ ] **Step 3: Lancer les tests pour vérifier qu'ils échouent**

```bash
pnpm test src/mailer/mailer.service.spec.ts
```

Expected: erreur `Cannot find module './mailer.service'`. Normal.

- [ ] **Step 4: Créer l'implémentation du service**

Créer `src/mailer/mailer.service.ts` :

```typescript
import { Inject, Injectable, Logger } from '@nestjs/common';
import type { Transporter } from 'nodemailer';
import { AppConfigService } from '../config/app-config.service';
import { MAIL_TRANSPORTER } from './mailer.constants';

const MAX_RETRIES = 3;

interface SendMailOptions {
  to: string;
  subject: string;
  html: string;
}

@Injectable()
export class MailerService {
  private readonly logger = new Logger(MailerService.name);

  constructor(
    @Inject(MAIL_TRANSPORTER) private readonly transporter: Transporter,
    private readonly cfg: AppConfigService,
  ) {}

  async sendMail({ to, subject, html }: SendMailOptions): Promise<void> {
    let lastError: unknown;
    for (let attempt = 1; attempt <= MAX_RETRIES; attempt++) {
      try {
        await this.transporter.sendMail({
          from: this.cfg.smtpFrom,
          to,
          subject,
          html,
        });
        return;
      } catch (err) {
        lastError = err;
        if (attempt < MAX_RETRIES) {
          await new Promise((resolve) =>
            setTimeout(resolve, 1000 * attempt),
          );
        }
      }
    }
    this.logger.error(
      `Failed to send email to ${to} after ${MAX_RETRIES} attempts`,
      lastError,
    );
    throw lastError;
  }
}
```

- [ ] **Step 5: Lancer les tests pour vérifier qu'ils passent**

```bash
pnpm test src/mailer/mailer.service.spec.ts
```

Expected: 6 tests verts.

> Si les tests timer-based posent problème (jest fake timers + async peut être fragile), un fallback acceptable est de **réduire le `MAX_RETRIES_DELAY` via une constante exportée et l'overrider dans les tests** — mais commencer par essayer la version fake-timers ci-dessus, qui devrait fonctionner avec Jest 29+.

- [ ] **Step 6: Vérifier la suite complète**

```bash
pnpm test
```

Expected: ~167 tests verts.

- [ ] **Step 7: Lint**

```bash
pnpm lint
```

Expected: clean.

- [ ] **Step 8: Commit**

```bash
git add src/mailer/mailer.constants.ts src/mailer/mailer.service.ts src/mailer/mailer.service.spec.ts
git commit -m "feat(mailer): MAIL_TRANSPORTER + MailerService (retry 3x backoff) + 6 tests"
```

---

## Task 5: MailerModule + wiring AppModule

**Files:**
- Create: `src/mailer/mailer.module.ts`
- Modify: `src/app.module.ts`

- [ ] **Step 1: Créer le module**

Créer `src/mailer/mailer.module.ts` :

```typescript
import {
  Global,
  Inject,
  Module,
  OnModuleDestroy,
  type Provider,
} from '@nestjs/common';
import { createTransport, type Transporter } from 'nodemailer';
import { AppConfigModule } from '../config/app-config.module';
import { AppConfigService } from '../config/app-config.service';
import { MAIL_TRANSPORTER } from './mailer.constants';
import { MailerService } from './mailer.service';

const transporterProvider: Provider = {
  provide: MAIL_TRANSPORTER,
  inject: [AppConfigService],
  useFactory: (cfg: AppConfigService): Transporter =>
    createTransport({
      host: cfg.smtpHost,
      port: cfg.smtpPort,
      secure: cfg.smtpSecure,
      auth: {
        user: cfg.smtpUser,
        pass: cfg.smtpPass,
      },
    }),
};

@Global()
@Module({
  imports: [AppConfigModule],
  providers: [transporterProvider, MailerService],
  exports: [MailerService],
})
export class MailerModule implements OnModuleDestroy {
  constructor(
    @Inject(MAIL_TRANSPORTER) private readonly transporter: Transporter,
  ) {}

  onModuleDestroy(): void {
    this.transporter.close();
  }
}
```

- [ ] **Step 2: Wirer dans `src/app.module.ts`**

Ouvrir `src/app.module.ts`. État actuel — la section des imports en haut (les exact `import` lines):

```typescript
import { ProjectsModule } from './projects/projects.module';
```

Ajouter immédiatement après cette ligne :

```typescript
import { MailerModule } from './mailer/mailer.module';
```

Et dans le tableau `imports` du `@Module`, l'état actuel se termine par :

```typescript
    StorageModule,
    ProjectsModule,
  ],
```

Le remplacer par (ajout `MailerModule` après `ProjectsModule`) :

```typescript
    StorageModule,
    ProjectsModule,
    MailerModule,
  ],
```

- [ ] **Step 3: Vérifier que TypeScript compile**

```bash
pnpm build
```

Expected: clean.

- [ ] **Step 4: Suite de tests**

```bash
pnpm test
```

Expected: ~167 tests verts (idem fin Task 4 — pas de nouveau test ici).

- [ ] **Step 5: Lint**

```bash
pnpm lint
```

Expected: clean.

- [ ] **Step 6: Smoke test boot**

S'assurer que Postgres + MinIO + Mailpit tournent :

```bash
pnpm db:up && pnpm db:wait
pnpm s3:up
pnpm mail:up
sleep 3
```

Puis démarrer l'app en mode dev avec un timeout de 25s pour capturer les logs de boot :

```bash
timeout 25 pnpm dev > /tmp/mailer-boot.log 2>&1 || true
grep -E 'MailerModule|MailerService|Mail|/health' /tmp/mailer-boot.log
```

Expected (au moins ces lignes) :
- `MailerModule dependencies initialized`
- `Nest application successfully started`

Pas de routes mappées sous `/mail` (le module n'expose pas d'endpoint HTTP).

Si `MailerModule dependencies initialized` n'apparaît pas, c'est qu'il y a un souci au boot — investiguer le log.

- [ ] **Step 7: Commit**

```bash
git add src/mailer/mailer.module.ts src/app.module.ts
git commit -m "feat(mailer): MailerModule @Global + wire dans AppModule"
```

---

## Task 6: Vérification e2e manuelle

**Files:** Aucun changement de code. Cette tâche valide que l'envoi de mail fonctionne réellement contre Mailpit.

- [ ] **Step 1: Démarrer la stack complète**

```bash
pnpm db:up && pnpm db:wait && pnpm s3:up && pnpm mail:up
sleep 3
```

- [ ] **Step 2: Vérifier que Mailpit est vide (page d'accueil sans messages)**

Dans le navigateur ou via curl :

```bash
curl -fsS http://localhost:8025/api/v1/messages | head -c 200
```

Expected: JSON avec `"messages": []` ou similaire (aucun message). Si des messages d'anciens tests existent, c'est OK — on va juste vérifier l'arrivée d'un nouveau.

- [ ] **Step 3: Démarrer l'app**

```bash
pnpm dev > /tmp/mailer-e2e-dev.log 2>&1 &
echo $! > /tmp/mailer-e2e-pid
sleep 8
```

Vérifier que l'app répond :

```bash
curl -fsS http://localhost:3000/health | head -c 200
```

Expected: `{"status":"ok"}` ou `{"status":"degraded"}`.

- [ ] **Step 4: Envoyer un mail de test via un script ad-hoc**

Créer un fichier temporaire `/tmp/mailer-e2e-test.ts` :

```typescript
import { NestFactory } from '@nestjs/core';
import { AppModule } from './src/app.module';
import { MailerService } from './src/mailer/mailer.service';

async function main() {
  const app = await NestFactory.createApplicationContext(AppModule, {
    logger: false,
  });
  const mailer = app.get(MailerService);
  await mailer.sendMail({
    to: 'test-visitor@example.com',
    subject: 'Mailer E2E Test',
    html: '<p>Hello from <b>NestJS Mailer</b> test.</p>',
  });
  console.log('Mail sent successfully.');
  await app.close();
}

main().catch((err) => {
  console.error('Mail failed:', err);
  process.exit(1);
});
```

> Note : ce script utilise le path `./src/app.module` parce qu'on l'exécute depuis la racine du projet. Si le `tsconfig` dérange, créer le fichier directement dans `src/` au lieu de `/tmp/` et adapter.

L'exécuter :

```bash
cd /home/jned/WebstormProjects/J-Ned/nest-portfolio-app
pnpm exec tsx /tmp/mailer-e2e-test.ts
```

Expected: sortie `Mail sent successfully.` puis exit 0.

> Si `tsx` ne trouve pas `./src/app.module` à cause du path `/tmp/`, déplacer le script dans le repo (ex: `src/mailer-e2e-test.ts` temporaire) puis le supprimer après.

- [ ] **Step 5: Vérifier que Mailpit a reçu le mail**

```bash
curl -s http://localhost:8025/api/v1/messages | grep -o '"Subject":"[^"]*"' | head
```

Expected: au moins une ligne `"Subject":"Mailer E2E Test"` dans la sortie.

Alternativement, ouvrir `http://localhost:8025` dans un navigateur et confirmer visuellement la présence du mail.

- [ ] **Step 6: Vérifier le contenu**

```bash
# Récupérer l'ID du mail le plus récent
LATEST_ID=$(curl -s 'http://localhost:8025/api/v1/messages?limit=1' | grep -o '"ID":"[^"]*"' | head -1 | sed 's/"ID":"//;s/"//')
curl -s "http://localhost:8025/api/v1/message/$LATEST_ID" | grep -E '"From"|"To"|"Subject"' | head
```

Expected:
- `"From"` contient `noreply@nedellec-julien.fr` (ou la valeur de `SMTP_FROM` configurée)
- `"To"` contient `test-visitor@example.com`
- `"Subject"` est `Mailer E2E Test`

- [ ] **Step 7: Cleanup**

```bash
kill $(cat /tmp/mailer-e2e-pid) 2>/dev/null
sleep 1
rm /tmp/mailer-e2e-test.ts /tmp/mailer-e2e-dev.log /tmp/mailer-e2e-pid 2>/dev/null
```

- [ ] **Step 8: Pas de commit**

Cette tâche ne modifie pas de code. Si tous les steps passent, marquer comme done.

---

## Task 7: README + clôture

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Ajouter la section Mailer**

Ouvrir `README.md`. Localiser la section `## Avatar Profile` (ajoutée au sous-projet précédent). Insérer la nouvelle section `## Mailer` **immédiatement après** la section Avatar Profile et **avant** `## Migration depuis le backend Hono`.

Contenu à insérer :

```markdown
## Mailer

Module d'infrastructure pour l'envoi d'emails SMTP. Utilisé par les feature modules qui ont besoin de notifier (Contact, Bookings, futurs modules).

**Stack** :
- Lib : `nodemailer` (SMTP transport)
- Dev local : container Mailpit (SMTP catch-all + web UI sur ports 1025/8025)
- Prod : SMTP utilisateur (Gmail / OVH / SES / etc.)

**Quickstart Mailer (en plus du DB et S3)** :

```bash
pnpm mail:up           # démarre Mailpit
pnpm mail:console      # affiche l'URL de la web UI
pnpm mail:logs         # tail logs Mailpit
```

> Le hook `predev` démarre automatiquement Postgres + MinIO + Mailpit avant `pnpm dev`. Onboarding : `pnpm install && cp .env.example .env && pnpm dev`.

**API** :

| Méthode | Signature |
|---|---|
| `sendMail` | `({ to, subject, html }): Promise<void>` (3 retries linear backoff 1s/2s/3s, throws final si tous échouent) |

**Helpers utilitaires** (depuis `src/mailer/mailer.utils.ts`) :

| Fonction | Signature |
|---|---|
| `renderTemplate` | `(html: string, vars: Record<string, string>): string` (string replace `{{var}}`) |
| `loadTemplate` | `(absolutePath: string): string` (sucre sur `readFileSync`) |

**Usage dans un feature module** :

```typescript
import { resolve } from 'node:path';
import { Injectable } from '@nestjs/common';
import { MailerService } from '../mailer/mailer.service';
import { loadTemplate, renderTemplate } from '../mailer/mailer.utils';

@Injectable()
export class ContactService {
  constructor(private readonly mailer: MailerService) {}

  async notifyAdmin(data: { name: string; email: string; message: string }): Promise<void> {
    const tmpl = loadTemplate(
      resolve(__dirname, 'mail-templates', 'contact-notification.html'),
    );
    const html = renderTemplate(tmpl, data);
    await this.mailer.sendMail({
      to: 'admin@nedellec-julien.fr',
      subject: `Nouveau message de ${data.name}`,
      html,
    });
  }
}
```

**`MailerModule` est `@Global`** : `MailerService` est injectable directement, pas besoin d'`imports: [MailerModule]` dans les feature modules.

**Configuration prod** :

```bash
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false   # true pour port 465 (TLS direct)
SMTP_USER=<...>
SMTP_PASS=<...>
SMTP_FROM=noreply@nedellec-julien.fr
```

**Voir le spec complet** : [`docs/superpowers/specs/2026-04-26-mailer-design.md`](docs/superpowers/specs/2026-04-26-mailer-design.md).
```

- [ ] **Step 2: Mettre à jour la liste des sous-projets**

Localiser la liste numérotée dans la section `## Migration depuis le backend Hono`. État actuel (après Avatar Profile, 10 items) :

```markdown
1. ✅ Fondations
2. ✅ Auth (Users + JWT + 2FA + backup codes)
3. ✅ Profile public (Profile, Hero, SocialLinks, Diplomas, Technologies, Expertises, ServicePricing)
4. ✅ S3 Storage (StorageModule + MinIO local + Garage prod)
5. ✅ Projects (CRUD + upload image qui consomme S3 Storage)
6. ✅ Avatar Profile (`POST /profile/avatar` + transformation key→URL en sortie API, cohérent Projects)
7. **Contact** *(prochain)* (messages + mailer)
8. Bookings (réservations + slots + mail)
9. CV (upload S3 + download)
10. Analytics (page views + agrégats)
```

Le remplacer par (insertion d'un nouvel item 7 "Mailer", décalage des suivants — la liste passe de 10 à 11 items) :

```markdown
1. ✅ Fondations
2. ✅ Auth (Users + JWT + 2FA + backup codes)
3. ✅ Profile public (Profile, Hero, SocialLinks, Diplomas, Technologies, Expertises, ServicePricing)
4. ✅ S3 Storage (StorageModule + MinIO local + Garage prod)
5. ✅ Projects (CRUD + upload image qui consomme S3 Storage)
6. ✅ Avatar Profile (`POST /profile/avatar` + transformation key→URL en sortie API, cohérent Projects)
7. ✅ Mailer (MailerModule @Global + Mailpit local + nodemailer)
8. **Contact** *(prochain)* (messages + 2 templates qui consomment Mailer)
9. Bookings (réservations + slots + 2 templates qui consomment Mailer)
10. CV (upload S3 + download)
11. Analytics (page views + agrégats)
```

- [ ] **Step 3: Sanity check**

Relire visuellement les sections modifiées :
- La section `## Mailer` est bien entre `## Avatar Profile` et `## Migration depuis le backend Hono`.
- Le tableau d'API rend correctement.
- Le lien spec pointe vers `2026-04-26-mailer-design.md` (date d'aujourd'hui).
- Item 7 est ✅ Mailer, item 8 est Contact en cours.

- [ ] **Step 4: Final check global**

```bash
pnpm lint && pnpm build && pnpm test
```

Expected: tout passe. ~167 tests verts.

- [ ] **Step 5: Vérifier les fichiers modifiés**

```bash
git diff --stat HEAD~6..HEAD
```

Expected (à 1-2 fichiers près selon l'ordre des commits) :
```
 .env                                                   |   8 +
 .env.example                                           |   8 +
 README.md                                              | ~50 +-
 compose.yaml                                           |  14 +
 package.json                                           |   8 +-
 pnpm-lock.yaml                                         | many +-
 src/app.module.ts                                      |   2 +
 src/config/app-config.service.ts                       |  18 +
 src/config/env.schema.ts                               |   8 +
 src/config/env.validation.spec.ts                      |  20 +-
 src/mailer/mailer.constants.ts                         | new
 src/mailer/mailer.module.ts                            | new
 src/mailer/mailer.service.spec.ts                      | new (~80 lignes)
 src/mailer/mailer.service.ts                           | new (~45 lignes)
 src/mailer/mailer.utils.spec.ts                        | new
 src/mailer/mailer.utils.ts                             | new
```

- [ ] **Step 6: Commit**

```bash
git add README.md
git commit -m "docs: README — section Mailer + liste sous-projets"
```

---

## Critères de done globaux (rappel du spec § 11)

Le sous-projet est terminé quand :

1. ✅ Dépendances `nodemailer` + `@types/nodemailer` installées (Task 1).
2. ✅ Env vars étendues (6 nouvelles, getters, tests, fail-fast) (Task 1).
3. ✅ `compose.yaml` étendu, `pnpm mail:up` opérationnel, web UI sur 8025 (Task 2).
4. ✅ `MailerModule @Global` wiré dans `AppModule`, boot logs OK (Task 5).
5. ✅ `MailerService.sendMail` avec retry 3x linear backoff (Task 4).
6. ✅ `OnModuleDestroy` appelle `transporter.close()` (Task 5).
7. ✅ Helpers `renderTemplate`/`loadTemplate` exportés (Task 3).
8. ✅ ~10 nouveaux tests verts, total ~167 (Task 1, 3, 4).
9. ✅ Build prod + lint clean (Task 7 step 4).
10. ✅ Vérification e2e manuelle (Task 6).
11. ✅ README mis à jour (Task 7).
